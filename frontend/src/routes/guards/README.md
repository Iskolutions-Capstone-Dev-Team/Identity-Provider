# Route Guards

Route guard components currently remain in `src/auth/components` because they are tightly coupled to the existing authentication, SSO, MFA, token, and session behavior.

Move them here only with dedicated auth regression testing.
