import { describe, it, expect } from 'vitest';
import * as modalTransition from '../modalTransition';

describe('modalTransition utils', () => {
  it('exports utilities', () => {
    expect(modalTransition).toBeDefined();
  });
});
