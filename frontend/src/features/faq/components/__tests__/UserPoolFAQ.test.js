import { describe, it, expect } from 'vitest';
import * as UserPoolFAQ from '../UserPoolFAQ';

describe('UserPoolFAQ', () => {
  it('exports FAQ', () => {
    expect(UserPoolFAQ).toBeDefined();
  });
});
