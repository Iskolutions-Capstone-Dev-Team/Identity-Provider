import { describe, it, expect } from 'vitest';
import * as AppClientFAQ from '../AppClientFAQ';

describe('AppClientFAQ', () => {
  it('exports FAQ', () => {
    expect(AppClientFAQ).toBeDefined();
  });
});
