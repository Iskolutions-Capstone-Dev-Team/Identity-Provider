import { describe, it, expect } from 'vitest';
import * as useRegistrationAccountTypes from '../useRegistrationAccountTypes';

describe('useRegistrationAccountTypes hook', () => {
  it('exports hook', () => {
    expect(useRegistrationAccountTypes).toBeDefined();
  });
});
