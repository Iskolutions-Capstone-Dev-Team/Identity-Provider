import { describe, it, expect } from 'vitest';
import * as reinviteAccountType from '../reinviteAccountType';

describe('reinviteAccountType utils', () => {
  it('exports utilities', () => {
    expect(reinviteAccountType).toBeDefined();
  });
});
