import { describe, it, expect } from 'vitest';
import * as userPoolConstants from '../userPoolConstants';

describe('userPoolConstants', () => {
  it('exports constants', () => {
    expect(userPoolConstants).toBeDefined();
  });
});
