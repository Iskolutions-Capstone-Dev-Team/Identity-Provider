import { describe, it, expect } from 'vitest';
import * as mfaFlow from '../mfaFlow';

describe('mfaFlow utils', () => {
  it('exports utilities', () => {
    expect(mfaFlow).toBeDefined();
  });
});
