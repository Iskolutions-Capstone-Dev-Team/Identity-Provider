import { describe, it, expect } from 'vitest';
import * as authorizeFlow from '../authorizeFlow';

describe('authorizeFlow utils', () => {
  it('exports utilities', () => {
    expect(authorizeFlow).toBeDefined();
  });
});
